# Tests for cloudflare-pages-turbo

These files provide contract or smoke fixtures that CI can inspect for `cloudflare-pages-turbo` without requiring a full consumer repository checkout.

- `tests/smoke`
