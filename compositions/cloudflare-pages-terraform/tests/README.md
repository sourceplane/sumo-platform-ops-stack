# Tests for cloudflare-pages-terraform

These files provide contract or smoke fixtures that CI can inspect for `cloudflare-pages-terraform` without requiring a full consumer repository checkout.

- `tests/smoke`
