# Tests for cloudflare-pages-turbo-terraform

These files provide contract or smoke fixtures that CI can inspect for `cloudflare-pages-turbo-terraform` without requiring a full consumer repository checkout.

- `tests/smoke`
